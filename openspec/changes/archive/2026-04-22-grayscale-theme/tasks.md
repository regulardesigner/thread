## 1. Light theme — grayscale base

- [x] 1.1 Replace `--bg` with `#f2f2f2` (soft off-white)
- [x] 1.2 Replace `--surface` with `#ffffff` and `--surface-soft` with `#f8f8f8`
- [x] 1.3 Replace `--border` with `#d0d0d0` (mid-gray)
- [x] 1.4 Replace `--text` with `#1a1a1a` and `--text-soft` with `#666666`
- [x] 1.5 Replace `--accent` with `#333333` and `--accent-strong` with `#111111`
- [x] 1.6 Replace `--shadow` with `0 8px 24px rgba(0,0,0,0.08)` (neutral, no blue tint)

## 2. Light theme — semantic colors

- [x] 2.1 Replace `--danger-bg` with `#fff0f0` and `--danger-text` with `#cc2222`
- [x] 2.2 Replace `--warn-bg` with `#fff0f0` and `--warn-text` with `#cc2222` (same as danger)
- [x] 2.3 Replace `--ok-bg` with `#f0fff4` and `--ok-text` with `#1a8a3a`

## 3. Dark theme — grayscale base

- [x] 3.1 Replace `--bg` with `#1e1e1e` (dark charcoal, not pure black)
- [x] 3.2 Replace `--surface` with `#282828` and `--surface-soft` with `#303030`
- [x] 3.3 Replace `--border` with `#444444`
- [x] 3.4 Replace `--text` with `#e8e8e8` and `--text-soft` with `#999999`
- [x] 3.5 Replace `--accent` with `#cccccc` and `--accent-strong` with `#eeeeee`

## 4. Dark theme — semantic colors

- [x] 4.1 Replace `--danger-bg` with `#2c1414` and `--danger-text` with `#ff6666`
- [x] 4.2 Replace `--warn-bg` with `#2c1414` and `--warn-text` with `#ff6666` (same as danger)
- [x] 4.3 Replace `--ok-bg` with `#0e2416` and `--ok-text` with `#44cc6a`

## 5. Verification

- [x] 5.1 Confirm light mode renders with gray backgrounds and no tinted hues
- [x] 5.2 Confirm dark mode renders with charcoal (not black) backgrounds
- [x] 5.3 Confirm error/danger banners show red text/background in both modes
- [x] 5.4 Confirm success banners show green text/background in both modes
- [x] 5.5 Confirm interactive elements (buttons, inputs, links) are visually distinct using grays only
